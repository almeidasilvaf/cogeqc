# Check if BUSCO is installed

Check if BUSCO is installed

## Usage

``` r
busco_is_installed()
```

## Value

Logical indicating whether BUSCO is installed or not.

## Examples

``` r
busco_is_installed()
#> [1] FALSE
```
